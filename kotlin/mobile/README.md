# Keygen Kotlin Mobile

Android Kotlin implementation for the authorized key-generation and trusted-node research application. The module remains isolated under `kotlin/mobile/` and preserves the repository's safe cryptographic boundaries.

The mobile entry point is prepared to consume the same deterministic vectors and Ed25519 node-identity model used by the Java implementation. No unauthorized credential recovery is included.
