# Cross-language crypto implementation

Python, Node.js, Java and C++ use the same deterministic interoperability envelope and public/synthetic conformance-vector model. Java/Koronos remains the semantic JVM track; C++ is the native track; Node.js is the integration track; Python remains the research/reference track.

Private-key recovery from arbitrary public addresses is excluded across all tracks.