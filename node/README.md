# Node.js interoperability track

Node.js ESM modules mirror the Java/Python/C++ interoperability contracts for deterministic key/address validation, public/synthetic vectors, audit metadata and C8192/R8192 telemetry. Wallet signing remains external and owner-controlled. No address-to-private-key recovery or seed guessing is implemented.