# Web Implementations

The browser implementation generates demonstration random values with the Web Crypto API. It is not a license-key bypass or secret-extraction service. Server-side PHP is limited to a service boundary and must not generate or expose privileged secrets.