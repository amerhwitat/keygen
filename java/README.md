# Java implementation

Java 21/Maven interoperability layer. The existing Java implementation remains authoritative for Java-side models; JavaFX applications should live under this language root and consume the common deterministic vector format.
