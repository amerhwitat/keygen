# Apple application

Native SwiftUI/Xcode shell for keygen/Koronos 128D research. Shared Java/Kotlin research logic remains separate; the Apple shell exposes deterministic, non-secret interoperability and trusted-node boundaries.

Targets: iOS/iPadOS and macOS. IPA export requires macOS/Xcode.
