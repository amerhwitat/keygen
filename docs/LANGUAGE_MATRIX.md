# Chimera language matrix

Python/Java remain reference and semantic layers; C++ and MSVC provide native paths; Node.js provides integration; C#/.NET 8/9/10 provides managed Windows/cross-platform tooling. Interop uses deterministic envelopes and signed/provenance-aware metadata.
